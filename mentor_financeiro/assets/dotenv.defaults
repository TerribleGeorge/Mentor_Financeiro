# Valores por omissão (sem segredos) — incluído no bundle para CI e `flutter test`.
# Para chaves reais em desenvolvimento, edite localmente sem commitar ou use CI.
#
# Opcional: cotações globais Alpha Vantage (prioridade sobre Yahoo quando definido).
ALPHA_VANTAGE_API_KEY=
# Um ou mais e-mails admin (premium), separados por vírgula.
FIREBASE_ADMIN_EMAIL=
